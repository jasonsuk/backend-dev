function getAllUsers(req, res) {
    res.status(500).json({
        status: 'fail',
        msg: 'Server not yet ready',
    });
}

function getSingleUser(req, res) {
    res.status(500).json({
        status: 'fail',
        msg: 'Server not yet ready',
    });
}

function addUser(req, res) {
    res.status(500).json({
        status: 'fail',
        msg: 'Server not yet ready',
    });
}

function updateUser(req, res) {
    res.status(500).json({
        status: 'fail',
        msg: 'Server not yet ready',
    });
}

function deleteUser(req, res) {
    res.status(500).json({
        status: 'fail',
        msg: 'Server not yet ready',
    });
}

module.export = {
    getAllUsers,
    getSingleUser,
    addUser,
    updateUser,
    deleteUser,
};
